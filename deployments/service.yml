---
apiVersion: v1
kind: Service
metadata:
  name: game-service
  labels:
    app: game-app
    env: prod
spec:
  type: LoadBalancer
  selector:
    app: game-app
    env: prod
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
